# Research Log - August 12th, 2026

## Today's Goals

The goals for today's work include polishing up and finishing the synthetic time series data by applying scaling onto the training features of the data function. Afterwards, I intend on implementing a simple linear regression and simple MLP model as a baseline to verify the performance of the MLP for future goals. 

Today's work will be considered complete once the following benchmarks are met:
 - Feature scaling is fit to the training data; the same scaling should be applied to validation and test splits
 - A passing test verifying that the training data is centered (mean is 0, and standard deviation is 1)
 - The linear regression model can train on the data and provide results
 - The linear regression model records and reports metrics such as Mean Absolute Error (MAE), Root Mean Squared Error (RMSE), and R-Squared (R2) on all splits
 - Target benchmark for linear regression: validation RMSE <= 3.0 and validation R2 >= 0.70
 - A passing test verifying that the linear regression model outperforms a simple mean predictor on the validation data
 - A simple PyTorch MLP can train on the synthetic dataset
 - The MLP reports MAE, RMSE, and R2 on all splits
 - The MLP should roughly match the linear regression model's results
 - A passing test that verifies that the MLP produces predictions with the expected shape
 - All tests pass with pytest
 - A short post-work note records which model performed better and whether the results were understandable and provides insight on originally unknown terms and concepts.

### Files

 - [src/xai_mini_research/preprocessing.py](../src/xai_mini_research/preprocessing.py)
 - [src/xai_mini_research/metrics.py](../src/xai_mini_research/metrics.py)
 - [src/xai_mini_research/models/__init__.py](../src/xai_mini_research/models/__init__.py)
 - [src/xai_mini_research/models/linear.py](../src/xai_mini_research/models/linear.py)
 - [src/xai_mini_research/models/mlp.py](../src/xai_mini_research/models/mlp.py)

 - [tests/test_preprocessing.py](../tests/test_preprocessing.py)
 - [tests/test_metrics.py](../tests/test_metrics.py)
 - [tests/test_linear.py](../tests/test_linear.py)
 - [tests/test_mlp.py](../tests/test_mlp.py)

### Current Uncertainties

I currently do not recall or know how to calculate MAE, RMSE, and R2, so they will be included in the [Relevant Knowledge](#relevant-knowledge) section.

## Relevant Knowledge

### Mean Absolute Error (MAE)

MAE is calculated by calculating the absolute difference between a predicted outcome versus the true outcome. These differences are then averaged by summing up all distances and dividing by the total number of data points. 

$$\frac{1}{n}\sum^n_{i=1}|y_i-\hat{y}_i|$$

$y_i$: the actual value
$\hat{y}_i$: the predicted value.

### Root Mean Squared Error (RMSE)

RMSE is $$\mathrm{RMSE}=\sqrt{\frac{1}{n}\sum_{i=1}^{n}\left(y_i - \hat{y}_i\right)^2}.$$

### R-Squared (R2)

$$R^2=1-\frac{\sum_{i=1}^{n}\left(y_i-\hat{y}_i\right)^2}{\sum_{i=1}^{n}\left(y_i-\bar{y}\right)^2}$$

$\overline y$: the mean of true values.

### Linear Regression

For $$\hat y_i = ax+b,$$
$$a = \frac{\overline c_{xy}}{\overline\sigma_x^2},\\ b = \overline \mu_y - a\overline\mu_x,$$

where $\overline c_{xy}$: the empirical covariance between inputs and output
$\overline\sigma^2$: the empirical variance
$\overline\mu:$ the empirical mean

Do note that this is a simple formalation of the linear regression. The implementation will utilize methods from scikit-learn.

## Post-Work Report 

### Completed Benchmarks

Today's work completed the preprocessing, metrics, and linear regression goals. Feature scaling is fit only on the training data and applied to the validation and test splits. Tests now verify that the scaled training data is centered, that validation and test splits are transformed with the same scaler, and that the original data dictionary is not modified by preprocessing.

The linear regression model can train on the processed data and produce predictions for all splits. The metrics functions can calculate MAE, RMSE, and R2, and the linear regression tests verify that the model outperforms a mean predictor on the validation split. The validation benchmark of RMSE <= 3.0 and R2 >= 0.70 is also covered by a test.

### Shifted Benchmarks

The simple MLP was not implemented today and will be shifted to the second project day of this week. This means that MLP training, MLP metrics, and the MLP prediction-shape test still need to be completed.

### Copying Dictionaries
Copy data into a new dict to add processed X. Do note that when copying, only top-level instances are copied, so all nested dicts must be copied over explicitly as well.

### Schedule Adjustment

Although today's goals originally included the implementation of both a simple linear regression model and a simple MLP, I was only able to complete the preprocessing, metrics, and linear regression components within the available time. As such, the MLP will be shifted to the second project day of this week.

This still keeps the project aligned with the overall two-month schedule. Since my personal weekly schedule only includes two project days per week, the goal for Week 3 will remain the implementation of the KRR baseline as stated in my original plan. However, if the simple MLP is completed on the second project day of this week, then the MLP goal from Week 4 will already have been knocked out early.

This means that the order of the milestones I have personally set has been slightly adjusted, but the overall direction of the project remains the same. 

## References
